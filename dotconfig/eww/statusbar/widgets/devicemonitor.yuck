(defwidget devicemonitor []
    (box
        :space-evenly false
        :spacing 6

        (box
            :space-evenly false

            ""
            "${round(EWW_CPU.avg, 0)}%"
        )
        (box
            :space-evenly false

            ""
            "${round(EWW_RAM.used_mem_perc, 0)}%"
        )
        (box
            :space-evenly false

            ""
            "${round(EWW_TEMPS.K10TEMP_TCTL, 0)}°C"
        )
    )
)
